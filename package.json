{
  "name": "elbow-room-sass",
  "version": "0.0.1",
  "description": "Provides unique classes to quickly add margin or padding (elbow room) to your layout via Sass/Css",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-sass": "^0.9.2",
    "grunt-contrib-watch": "^0.6.1"
  }
}
